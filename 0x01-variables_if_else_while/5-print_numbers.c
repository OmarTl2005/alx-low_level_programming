#include <stdio.h>
/**
  *main - Entry point
  *Description: 'This program will print all single digits of base 10'
  *Return: always 0
  */

int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d \n", num);
	}
	return (0);
}
